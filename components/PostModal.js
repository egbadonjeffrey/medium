const styles = {
  wrapper: `w-[50rem]  h-[50rem] flex-col justify-start items-center gap-[1rem] p-[1rem] font-mediumSerif overflow-scroll`,
};

const PostModal = () => {
  return <div className={styles.wrapper}>Modal</div>;
};

export default PostModal;
